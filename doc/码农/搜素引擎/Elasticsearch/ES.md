一次检索请求的流程

倒排索引

Shard 和 Replicas 的含义

Segment 文件会一直增加吗？

为什么 ElasticSearch 是近实时的？

2.ES可以支持集群 但是最多支持上百台的节点 相对于HDFS上千台集群规模较少 不太适合海量数据

3.文档：
  创建索引 需要文档中的每个字段的数据类型 并且指定不同的分析器
  在存储结构上 由_index、 _type 和_id 唯一标识一个文档。
  