Linux的select、poll、epoll的区别
linux零拷贝
epoll原理，底层数据结构，LT和ET
讲讲select，为什么不用，如果只有一个文件描述符，哪个快
select的底层实现
epoll的触发方式，有什么区别？
水平触发和边缘触发的应用场景
阻塞和非阻塞的区别
同步和异步IO的区别
select、poll、epoll的实现和应用
你说epoll用到红黑树，那你讲一下红黑树

为什么关闭一个套接字后，马上打开会报错？如何解决？（套接字复用）


1.如何保持长连接 ？HTTP协议中的长连接与TCP/IP协议长连接的关系