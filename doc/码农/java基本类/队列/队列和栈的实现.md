1.栈的实现  栈和队列的标准API
 Stack 
 基本方法
 push  进栈
 peek 获取栈顶元素 但是不移除
 pop 获取栈顶元素 并且移除栈定元素
 
2.队列的实现 
 Queue
   基本方法
 add   插入队列
 offer 插入队列  和add 的区别在于 在容量受限的情况下 会报异常
 remove 获取队列的头并且移除 
 poll  获取并且移除队列的头
 element  获取队列的头 不移除
 peek  获取队列的头 但是不移除 
 