问题 mysql里面有2000W的数据 redis只能保存20w  怎么保证redis里面都是热点数据

提供一种类似LRU算法的缓存实现机制 每次redis的命中 就增加一定的ttl时间
 
 如何实现自己的LRU算法：
 一文讲透Redis里面也在使用的LRU缓存算法
 做为最近最少使用算法   当put数据时没有 就采用头插法法  get数据时没有返回null 有的话返回值 并且把这个命中数据放到头 即可
 
 
 
 2.一条redis高延迟的原因是什么？
  redis高延迟的原因一般有以下几种不合理使用数据结构，比如对于有些hash类型，在查找时比较耗费性能;一些持久化带来的高延迟原因;内存交换导致延迟
  
  
3.redis的淘汰策略  实现一个LRU算法？
redis采取的是一种类似redis的算法  每次选择固定的数量的key(可以动态配置)， 然后在其中选择最近最少使用的进行淘汰
这个算法有缺点 

新的版本里面增加LFU算法  核心思想是根据key最近访问的频率进行淘汰